services:
  app:
    build: .
    restart: unless-stopped
    container_name: unitask-app
    env_file:
      - .env
    volumes:
      - .:/var/www
    ports:
      - "8000:8000"
    command: php artisan serve --host=0.0.0.0 --port=8000